local StarterPlayer = game:GetService("StarterPlayer")

local PlayerUtils = require(StarterPlayer.StarterPlayerScripts.ClientUtils.PlayerUtils)

local playerGui = PlayerUtils.playerGui

return {
	INTERFACE_PARENT = playerGui;
}